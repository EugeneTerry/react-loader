import Loading from "./Loading";

const App = () => {
  
  return (
  //Loading is a component that takes two props, startText and stopText.
    <Loading
      startText='Start Loading'
      stopText='Stop Loading'
    />
  )
}

export default App;
